import 'package:flutter/material.dart';
class TshirtTile extends StatelessWidget {
  const TshirtTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}